{
  "schemaVersion": 1,
  "id": "openzoo",
  "name": "openzoo",
  "version": "0.2.0",
  "author": "stacc",
  "license": "MIT",
  "repository": "https://github.com/staccDOTsol/omarchy-openzoo-plugin",
  "description": "Live openzoo spend on the bar, and a question box that answers without a terminal \u2014 paid per call by x402 from a local burner wallet, no account and no API key. Optionally also does semantic recall over a holographically-bound corpus when a leCore daemon is running.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "openzoo",
    "description": "Live spend and a question box, plus optional local corpus search.",
    "category": "Productivity",
    "defaultSection": "center",
    "allowMultiple": false,
    "defaults": {
      "proxy": "http://localhost:8402",
      "model": "deepseek/deepseek-v4-pro-0813",
      "endpoint": "http://127.0.0.1:8787",
      "token": "hrr-lab-token",
      "tenantId": "claude-code",
      "contextId": "",
      "topK": 8,
      "hostedKey": ""
    },
    "schema": [
      {
        "key": "proxy",
        "type": "string",
        "label": "openzoo proxy",
        "description": "Local x402-paying proxy, started by `openzoo claude`. Localhost is keyless by design, so there is no token here."
      },
      {
        "key": "model",
        "type": "string",
        "label": "Ask model",
        "description": "Which model answers the question box. Any id from GET /v1/models."
      },
      {
        "key": "endpoint",
        "type": "string",
        "label": "leCore daemon",
        "description": "Local holographic memory daemon. Default is loopback \u2014 nothing leaves this machine."
      },
      {
        "key": "token",
        "type": "string",
        "label": "Daemon token",
        "description": "Bearer token the local daemon expects."
      },
      {
        "key": "tenantId",
        "type": "string",
        "label": "Tenant",
        "description": "Namespace within the daemon."
      },
      {
        "key": "contextId",
        "type": "string",
        "label": "Context id",
        "description": "Which bound corpus to search (ctx_...). Required \u2014 the daemon has no list endpoint."
      },
      {
        "key": "topK",
        "type": "int",
        "label": "Retrieval breadth",
        "description": "Slices returned per query. Breadth is the real quality knob."
      },
      {
        "key": "hostedKey",
        "type": "string",
        "label": "openzoo key (optional)",
        "description": "LEAVE EMPTY FOR FULLY LOCAL. Setting an ozk_live_ key opts in to hosted models and cross-machine corpora, and sends queries off this machine."
      }
    ]
  }
}